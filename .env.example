# Copy this file to .env.local. Never commit the real key.
VITE_GOOGLE_MAPS_API_KEY=replace_with_your_restricted_browser_key
# Optional. When omitted, the app uses the checked-in Sprint 1 sample places.
# The endpoint should respond to GET /accessible-places with a JSON array.
VITE_ACCESSIBLE_PLACES_API_URL=
